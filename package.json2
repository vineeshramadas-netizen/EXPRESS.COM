cat > package.json <<'JSON'
{
  "name": "express.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
JSON
# then test locally:
npm install
npm run build
# commit & push if you want Vercel to retry
git add package.json package-lock.json
git commit -m "fix: restore valid package.json for build"
git push
